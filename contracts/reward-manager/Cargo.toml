[package]
name = "reward-manager"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Soroban contract that coordinates reward distribution for Hunty."
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "26.0.0-rc.1", features = ["testutils"] }
